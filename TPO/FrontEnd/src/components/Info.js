import { StyleSheet, Text, View} from 'react-native';

function Info(){
  return (
    <View>
        <Text></Text>
    </View>
  );
};

const styles = StyleSheet.create({
  });
  
export default Info;