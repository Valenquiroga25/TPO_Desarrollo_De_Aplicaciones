import { StyleSheet } from 'react-native';

const buttonStyles = StyleSheet.create({
      buttonWrapper: {
        padding: 10,
      },
      clickableImage: {
        width: "100%", 
        height: 110, 
        borderRadius: 10,
        borderColor: '#333333',
        borderWidth: 1,
      },
    });
export default buttonStyles;